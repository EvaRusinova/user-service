h2:
  console:
    enabled: true
jpa:
  show_sql: false
  database: H2
  open-in-view: false
  hibernate:
    ddl-auto: create-drop
datasource:
  url: jdbc:h2:mem:testdb;MODE=MySQL;DB_CLOSE_ON_EXIT=FALSE;IGNORECASE=TRUE
  username: sa
  initialize: false